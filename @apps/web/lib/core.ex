defmodule Core do
  @moduledoc """
  Domain layer for the ~/.claude overlay: transcripts, memory banks, the diary
  (`UserLog`), launchd routines, and the filesystem watcher that feeds PubSub.
  """
end
