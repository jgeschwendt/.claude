# Submit — learnings queue

Continuous captures from `/submit` runs (see SKILL.md → Learning) — appended inline the moment a run
goes off-playbook. Promotion (start of an interactive run) drains to the routed destination, stamps
`(since <date> · <artifact>)`, and checks the entry off.

Format: `- [ ] (YYYY-MM-DD · <artifact>) → <dest>: <one-line change>`
Destinations: **this SKILL.md** · **the repo** (`.claude/`) · **user memory**.

<!-- captures below -->
